[project]
name = "expense-tracker-backend"
version = "0.1.0"
description = "A backend application for tracking expenses."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi == 0.136.3",
    "sqlmodel == 0.0.38",
    "pydantic == 2.13.4",
    "uvicorn == 0.49.0",
    "structlog == 26.1.0",
    "python-dotenv == 1.2.2",
]
